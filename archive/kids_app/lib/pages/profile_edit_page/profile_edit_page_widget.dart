import 'package:flutter/material.dart';

class ProfileEditPageWidget extends StatelessWidget {
  const ProfileEditPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("profile page edit widget"),
    );
  }
}
